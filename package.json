{
  "name": "github-project-discovery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "discover": "node scripts/discover.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm test"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
